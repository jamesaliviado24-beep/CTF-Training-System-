# Linux Quick Reference (Bandit)

ls -la → show hidden files
cat filename → read file
file filename → check file type
find . -size 1033c → find file by size
grep "word" file → search inside file
sort file | uniq -u → find unique line
strings file → extract readable text
base64 -d file → decode base64
2>/dev/null → hide error messages
